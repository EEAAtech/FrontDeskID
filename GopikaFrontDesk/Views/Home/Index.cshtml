@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Guest ID Repository</h1>
    <h2>&nbsp;</h2>
    @using (Html.BeginForm("Index", "Guests", FormMethod.Post, new { @class = "form-horizontal" }))
            {
        <div class="form-group">
            @*<label class="control-label col-md-4" for="FromDt">Start Date: </label>*@
            
                <input style="width:800px; !important; align-self:center !important;" name="searchString" id="searchString" type="text" />
            
        </div>
                <div class="form-group">
                    <div class="col-sm-8">
                        <button type="submit" class="btn btn-primary btn-lg">Search</button>
                    </div>
                </div>

    }
    
</div>

